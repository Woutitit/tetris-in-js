// an Engine can be an object literal since each game will only ever use one
var Engine = {
	update: function() {
		// Update physics.
	},

	render: function() {
		// Render entities.
	}
}

export default Engine;